# Face Swap Lab (InsightFace)

โปรเจกต์ตัวอย่างนี้สาธิตขั้นตอนการสลับใบหน้าด้วย [InsightFace](https://github.com/deepinsight/insightface) โดยใช้โมเดล `inswapper_128.onnx` บน Python, OpenCV และ ONNX Runtime ผ่านโน้ตบุ๊ก [`labs/face-swap.ipynb`](labs/face-swap.ipynb) เหมาะสำหรับงานทดลองด้าน Computer Vision หรือเพื่อใช้เป็นฐานต่อยอดระบบ face swap แบบกำหนดเอง

## Project Structure

```text
face-swap/
├── images/               # รูปตัวอย่าง (source / target)
├── labs/face-swap.ipynb  # โน้ตบุ๊กหลักที่รวมทุกขั้นตอน
├── models/               # โฟลเดอร์เก็บโมเดล ONNX
└── requirements.txt      # รายชื่อไลบรารีที่ต้องใช้
```

* `source_img` คือภาพใบหน้าที่เราต้องการนำไปวาง (identity ต้นฉบับ)
* `target_img` คือภาพปลายทางที่ต้องการแทนที่ใบหน้า
ภายในโน้ตบุ๊กได้กำหนดภาพจาก `images/source.png` และ `images/target.png` เป็นตัวอย่าง คุณสามารถสลับหรือเปลี่ยนเส้นทางไฟล์ได้ตามต้องการ

## System Requirements

- Python 3.9 ขึ้นไป (แนะนำให้ใช้ virtual environment)
- pip สำหรับติดตั้งแพ็กเกจ
- การ์ดจอที่รองรับ CUDA เป็นทางเลือก (สำหรับ `ctx_id=0` และ `onnxruntime-gpu`) หากไม่มีสามารถรันด้วย CPU ได้
- Jupyter Notebook หรือ JupyterLab สำหรับเปิดไฟล์ `.ipynb`

## Installation and Setup

1. สร้างและเปิดใช้งาน virtual environment (ตัวอย่างบน macOS / Linux)
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   ```
2. ติดตั้งไลบรารีหลัก
   ```bash
   pip install -r requirements.txt
   ```
3. หากมี GPU และต้องการใช้ CUDA provider ให้ติดตั้ง `onnxruntime-gpu` และไลบรารี CUDA ที่รองรับเพิ่มเติม จากนั้นสามารถลบบรรทัดคอมเมนต์ใน `requirements.txt` แล้วติดตั้งใหม่
4. ผู้ใช้ต้องดาวน์โหลดโมเดล `inswapper_128.onnx` จาก InsightFace ด้วยตนเองตามเงื่อนไข non-commercial license จากนั้นวางไว้ในโฟลเดอร์ `models/`

## Contributing

ผมยังต้องฝึกอีกเยอะ และโปรเจคนี้ก็ยังมีช่องว่างให้พัฒนาอีกมากครับ
หากคุณพบเห็นส่วนไหนที่สามารถปรับปรุงให้ดีขึ้นได้ — ไม่ว่าจะเล็กหรือใหญ่ — ยินดีรับทุกความช่วยเหลือจากทุก ๆ ท่านเลยครับ 🙏

ขั้นตอนร่วมสนับสนุน:

1. Fork repo นี้ไปที่ของคุณ
2. สร้าง branch สำหรับส่วนที่คุณอยากปรับปรุง
3. ลงมือเขียนโค้ด หรือเพิ่มเทสท์ หากจำเป็น
4. ส่ง Pull Request พร้อมเล่าว่าคุณปรับปรุงอะไร อย่างไรบ้าง และทำไมถึงสำคัญ

ทุกการมีส่วนร่วม ทั้งโค้ด ไอเดีย หรือ feedback ช่วยให้โปรเจคนี้เติบโตเร็วขึ้นมากครับ
ขอบคุณที่มาช่วยกันสร้างสิ่งนี้ให้ดีขึ้นไปด้วยกันครับ 🩵

---

**🧑‍💻 Happy Hacking!**
